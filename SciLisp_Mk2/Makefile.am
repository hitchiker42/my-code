#use recursive make for programs, but just but documentation rules here
#define subdirectories, and the order to make them in
#'.' is the current(top) directory
SUBDIRS = src . tests doc
ACLOCAL_AMFLAGS=-I m4
all-local:
	 cp src/Scilisp .
